module.exports = {

"[project]/.next-internal/server/app/api/projects/[id]/scans/route/actions.js [app-rsc] (server actions loader, ecmascript)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
}}),
"[externals]/next/dist/compiled/next-server/app-route.runtime.dev.js [external] (next/dist/compiled/next-server/app-route.runtime.dev.js, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route.runtime.dev.js"));

module.exports = mod;
}}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}}),
"[externals]/next/dist/compiled/next-server/app-page.runtime.dev.js [external] (next/dist/compiled/next-server/app-page.runtime.dev.js, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page.runtime.dev.js"));

module.exports = mod;
}}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}}),
"[externals]/stream [external] (stream, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}}),
"[externals]/http [external] (http, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("http", () => require("http"));

module.exports = mod;
}}),
"[externals]/url [external] (url, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("url", () => require("url"));

module.exports = mod;
}}),
"[externals]/punycode [external] (punycode, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("punycode", () => require("punycode"));

module.exports = mod;
}}),
"[externals]/https [external] (https, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("https", () => require("https"));

module.exports = mod;
}}),
"[externals]/zlib [external] (zlib, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("zlib", () => require("zlib"));

module.exports = mod;
}}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}}),
"[project]/src/lib/supabase/server.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "createClient": (()=>createClient)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$supabase$2f$ssr$2f$dist$2f$module$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$module__evaluation$3e$__ = __turbopack_context__.i("[project]/node_modules/@supabase/ssr/dist/module/index.js [app-route] (ecmascript) <module evaluation>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$supabase$2f$ssr$2f$dist$2f$module$2f$createServerClient$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@supabase/ssr/dist/module/createServerClient.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/headers.js [app-route] (ecmascript)");
;
;
async function createClient(admin = false) {
    for (const env of [
        "NEXT_PUBLIC_SUPABASE_URL",
        "NEXT_PUBLIC_SUPABASE_ANON_KEY",
        "SUPABASE_SERVICE_ROLE_KEY"
    ]){
        if (!process.env[env]) {
            throw new Error(`Missing env.${env}`);
        }
    }
    const cookieStore = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["cookies"])();
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$supabase$2f$ssr$2f$dist$2f$module$2f$createServerClient$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createServerClient"])(("TURBOPACK compile-time value", "https://htpiytutvbshtojgbxwy.supabase.co"), admin ? process.env.SUPABASE_SERVICE_ROLE_KEY : ("TURBOPACK compile-time value", "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imh0cGl5dHV0dmJzaHRvamdieHd5Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDE5MDQ1MjUsImV4cCI6MjA1NzQ4MDUyNX0.RzEXSbKOIcEpj0rX58H1vmFHoNdn6C3k1geg5Gfg8Nc"), {
        cookies: {
            getAll () {
                return cookieStore.getAll();
            },
            setAll (cookiesToSet) {
                try {
                    cookiesToSet.forEach(({ name, value, options })=>cookieStore.set(name, value, options));
                } catch  {
                // The `setAll` method was called from a Server Component.
                // This can be ignored if you have middleware refreshing
                // user sessions.
                }
            }
        }
    });
}
}}),
"[project]/src/lib/octokit/octokit.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "getOctokitRepo": (()=>getOctokitRepo)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/supabase/server.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$octokit$2f$rest$2f$dist$2d$src$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@octokit/rest/dist-src/index.js [app-route] (ecmascript)");
;
;
async function getOctokitRepo(projectId) {
    // Create server-side Supabase client
    const supabase = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createClient"])();
    // Get authenticated user data
    const { data: { user }, error: userError } = await supabase.auth.getUser();
    if (userError || !user) {
        throw new Error("Authentication required");
    }
    // Fetch the project data
    const { data: projectData, error: projectError } = await supabase.from("projects").select("*").eq("id", projectId).single();
    if (projectError || !projectData) {
        throw new Error("Project not found");
    }
    // Check if the user owns this project
    if (projectData.user_id !== user.id) {
        throw new Error("You don't have access to this project");
    }
    // Get the repository full name from project data
    const repoFullName = projectData.repository_full_name;
    // Get the GitHub access token
    const { data: githubProfile, error: githubProfileError } = await supabase.from("github_profiles").select("github_access_token").eq("user_id", user.id).single();
    if (githubProfileError || !githubProfile || !githubProfile.github_access_token) {
        throw new Error("GitHub token not found");
    }
    const githubToken = githubProfile.github_access_token;
    // Initialize Octokit with the token
    const octokit = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$octokit$2f$rest$2f$dist$2d$src$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Octokit"]({
        auth: githubToken
    });
    return {
        octokit,
        repoFullName
    };
}
}}),
"[project]/src/app/api/projects/[id]/repository/content/services.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "getFileContent": (()=>getFileContent)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$octokit$2f$octokit$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/octokit/octokit.ts [app-route] (ecmascript)");
;
async function getFileContent({ projectId, path, branch, commit }) {
    const { octokit, repoFullName } = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$octokit$2f$octokit$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getOctokitRepo"])(projectId);
    // Extract owner and repo from the full name
    const [owner, repo] = repoFullName.split("/");
    try {
        // Get repository content based on path, branch/commit
        const response = commit ? await octokit.repos.getContent({
            owner,
            repo,
            path,
            ref: commit
        }) : await octokit.repos.getContent({
            owner,
            repo,
            path,
            ref: branch || undefined
        });
        const content = response.data;
        // Check if we got a file or a directory
        if (Array.isArray(content)) {
            throw new Error("Path points to a directory, not a file");
        }
        // For file content
        if ("content" in content) {
            // Decode base64 content
            const fileContent = Buffer.from(content.content, "base64").toString("utf-8");
            return {
                name: content.name,
                path: content.path,
                size: content.size,
                content: fileContent,
                sha: content.sha,
                url: content.html_url,
                download_url: content.download_url,
                encoding: content.encoding,
                type: content.type
            };
        } else {
            throw new Error("Content is not available");
        }
    } catch (error) {
        // Handle specific errors from GitHub API
        if (error && typeof error === "object" && "status" in error && error.status === 404) {
            throw new Error("File not found in repository");
        }
        throw error;
    }
}
}}),
"[project]/src/lib/services/serverless/scanner/agents/dependency-retriever-agent.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "getParentFiles": (()=>getParentFiles)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$app$2f$api$2f$projects$2f5b$id$5d2f$repository$2f$content$2f$services$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/app/api/projects/[id]/repository/content/services.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$octokit$2f$octokit$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/octokit/octokit.ts [app-route] (ecmascript)");
;
;
async function getParentFiles(projectId, branch, commit, path) {
    const { octokit, repoFullName } = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$octokit$2f$octokit$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getOctokitRepo"])(projectId);
    try {
        // Get the target file's content
        const fileContent = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$app$2f$api$2f$projects$2f5b$id$5d2f$repository$2f$content$2f$services$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getFileContent"])({
            projectId,
            path,
            branch,
            commit
        });
        if (!fileContent.content) {
            throw new Error("File content not found");
        }
        // Parse imports from the file content
        const importPatterns = [
            // JavaScript/TypeScript - Handle both named and default imports
            /import\s+(?:\*\s+as\s+\w+\s+from\s+['"]([^'"]+)['"]|{[^}]+}\s+from\s+['"]([^'"]+)['"]|[\w*]+\s+from\s+['"]([^'"]+)['"]|['"]([^'"]+)['"])/g,
            /require\s*\(\s*['"]([^'"]+)['"]\s*\)/g
        ];
        // Map to track imports with their full paths and directories
        const imports = new Map();
        // Get the current directory for relative imports
        const currentDir = path.split("/").slice(0, -1).join("/");
        for (const pattern of importPatterns){
            let match;
            while((match = pattern.exec(fileContent.content)) !== null){
                // Clean up the import path - use the first non-null capture group
                const originalPath = (match[1] || match[2] || match[3] || match[4]).trim();
                let importPath = originalPath;
                // Handle relative imports
                if (importPath.startsWith("./") || importPath.startsWith("../")) {
                    importPath = `${currentDir}/${importPath}`.replace(/\/\.\//g, "/").replace(/\/[^\/]+\/\.\.\//g, "/");
                }
                // Remove any query parameters or fragments
                importPath = importPath.split("?")[0].split("#")[0];
                // Skip external imports (node_modules and built-ins)
                if (importPath.includes("node_modules") || !(importPath.startsWith(".") || importPath.startsWith("/") || importPath.includes("/"))) {
                    continue;
                }
                // Get import directory and name
                const importParts = importPath.split("/");
                const importName = importParts.pop()?.split(".")[0] || "";
                const importDir = importParts.join("/");
                // Save the import with its normalized path and directory
                imports.set(importName, {
                    fullPath: importPath,
                    directory: importDir
                });
            }
        }
        // If no imports found, return empty array
        if (imports.size === 0) {
            return [];
        }
        // Search the repository for files matching the imports
        const parentFiles = [];
        const searchQuery = Array.from(imports.keys()).map((name)=>`filename:${name}`).join(" OR ");
        const { data: searchResults } = await octokit.search.code({
            q: `repo:${repoFullName} ${searchQuery}`
        });
        // Filter results to only include files that match the import paths
        for (const item of searchResults.items){
            const filePath = item.path;
            const filePathParts = filePath.split("/");
            const fileName = filePathParts.pop() || "";
            const fileBaseName = fileName.split(".")[0];
            const fileDir = filePathParts.join("/");
            // Check if file matches any of our imports
            for (const [importName, importData] of imports.entries()){
                if (fileBaseName !== importName) continue;
                // Calculate directory match score (higher is better)
                let score = 0;
                // Exact directory match is best
                if (fileDir === importData.directory) {
                    score = 100;
                } else if (!importData.directory && fileDir === currentDir) {
                    score = 90;
                } else if (importData.directory && fileDir.includes(importData.directory)) {
                    score = 70;
                } else if (importData.directory && importData.directory.includes(fileDir)) {
                    score = 60;
                } else {
                    score = 10;
                }
                // For same-directory imports (like "./utils"), the directory match is critical
                if (importData.fullPath.startsWith("./") && fileDir !== currentDir && score < 90) {
                    continue;
                }
                if (score >= 50) {
                    parentFiles.push(filePath);
                }
            }
        }
        // Post-processing: remove duplicates
        const uniqueFiles = [
            ...new Set(parentFiles)
        ];
        return uniqueFiles;
    } catch (error) {
        // Handle specific errors from GitHub API
        if (error && typeof error === "object" && "status" in error && error.status === 404) {
            throw new Error("File not found in repository");
        }
        throw error;
    }
}
}}),
"[project]/src/lib/ai/providers/registry.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "ProviderRegistry": (()=>ProviderRegistry)
});
class ProviderRegistry {
    static instance;
    providers = new Map();
    constructor(){}
    static getInstance() {
        if (!ProviderRegistry.instance) {
            ProviderRegistry.instance = new ProviderRegistry();
        }
        return ProviderRegistry.instance;
    }
    registerProvider(config) {
        this.providers.set(config.name, config);
    }
    getProvider(name) {
        return this.providers.get(name);
    }
    getProviderByModel(model) {
        return Array.from(this.providers.values()).find((provider)=>Object.values(provider.modelEnum).includes(model));
    }
}
}}),
"[externals]/util [external] (util, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("util", () => require("util"));

module.exports = mod;
}}),
"[externals]/node:fs [external] (node:fs, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("node:fs", () => require("node:fs"));

module.exports = mod;
}}),
"[externals]/node:stream [external] (node:stream, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("node:stream", () => require("node:stream"));

module.exports = mod;
}}),
"[externals]/node:stream/web [external] (node:stream/web, cjs)": (function(__turbopack_context__) {

var { g: global, __dirname, m: module, e: exports } = __turbopack_context__;
{
const mod = __turbopack_context__.x("node:stream/web", () => require("node:stream/web"));

module.exports = mod;
}}),
"[project]/src/lib/ai/base.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "BaseAIService": (()=>BaseAIService)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$openai$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/openai/index.mjs [app-route] (ecmascript) <locals>");
;
class BaseAIService {
    client;
    apiKeys;
    currentKeyIndex = 0;
    baseURL;
    constructor(apiKeys, baseURL){
        if (!apiKeys.length) {
            throw new Error("No API keys provided");
        }
        this.apiKeys = apiKeys;
        this.baseURL = baseURL;
        this.client = this.createClient();
    }
    createClient() {
        return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$openai$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["default"]({
            apiKey: this.getNextApiKey(),
            baseURL: this.baseURL
        });
    }
    getNextApiKey() {
        const key = this.apiKeys[this.currentKeyIndex];
        this.currentKeyIndex = (this.currentKeyIndex + 1) % this.apiKeys.length;
        return key;
    }
    async sleep(ms) {
        return new Promise((resolve)=>setTimeout(resolve, ms));
    }
    async createCompletion(model, params, retryOptions = {}) {
        const options = {
            maxAttempts: 3,
            initialDelayMs: 1000,
            maxDelayMs: 10000,
            backoffFactor: 2,
            ...retryOptions
        };
        let lastError = null;
        let attempt = 0;
        while(attempt < options.maxAttempts){
            try {
                // If model is undefined, use a default model from the provider
                const modelName = model ? this.getModelName(model) : this.getDefaultModel();
                return await this.client.chat.completions.create({
                    ...params,
                    model: modelName
                });
            } catch (error) {
                lastError = error;
                if (error?.status !== 429) {
                    throw error;
                }
                attempt++;
                if (attempt === options.maxAttempts) {
                    throw lastError;
                }
                const delay = Math.min(options.initialDelayMs * Math.pow(options.backoffFactor, attempt - 1), options.maxDelayMs);
                // Create new client with next API key
                this.client = this.createClient();
                await this.sleep(delay);
            }
        }
        throw lastError;
    }
    // Get a default model to use when none is specified
    getDefaultModel() {
        // This should be overridden by each provider
        throw new Error("Default model not implemented");
    }
}
}}),
"[project]/src/lib/ai/providers/groq.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "GroqModel": (()=>GroqModel),
    "GroqService": (()=>GroqService)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$base$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/base.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$registry$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/registry.ts [app-route] (ecmascript)");
;
;
var GroqModel = /*#__PURE__*/ function(GroqModel) {
    GroqModel["DEEPSEEK_R1_DISTILL_LLAMA_70B"] = "deepseek-r1-distill-llama-70b";
    GroqModel["LLAMA_3_3_70B_VERSATILE"] = "llama-3.3-70b-versatile";
    return GroqModel;
}({});
class GroqService extends __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$base$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["BaseAIService"] {
    constructor(){
        const apiKeys = [
            process.env.GROQ_API_KEY_1,
            process.env.GROQ_API_KEY_2
        ].filter(Boolean);
        super(apiKeys, "https://api.groq.com/v1");
    }
    getModelName(model) {
        return model;
    }
    getDefaultModel() {
        return "deepseek-r1-distill-llama-70b";
    }
}
// Register the provider
__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$registry$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ProviderRegistry"].getInstance().registerProvider({
    name: "groq",
    serviceClass: GroqService,
    modelEnum: GroqModel
});
}}),
"[project]/src/lib/ai/providers/anthropic.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "AnthropicModel": (()=>AnthropicModel),
    "AnthropicService": (()=>AnthropicService)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$base$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/base.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$registry$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/registry.ts [app-route] (ecmascript)");
;
;
var AnthropicModel = /*#__PURE__*/ function(AnthropicModel) {
    AnthropicModel["CLAUDE_3_5_SONNET"] = "claude-3-5-sonnet-latest";
    AnthropicModel["CLAUDE_3_7_SONNET"] = "claude-3-7-sonnet-latest";
    return AnthropicModel;
}({});
class AnthropicService extends __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$base$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["BaseAIService"] {
    constructor(){
        const apiKeys = [
            process.env.ANTHROPIC_API_KEY_1,
            process.env.ANTHROPIC_API_KEY_2
        ].filter(Boolean);
        super(apiKeys, "https://api.anthropic.com/v1");
    }
    getModelName(model) {
        return model;
    }
    getDefaultModel() {
        return "claude-3-5-sonnet-latest";
    }
}
// Register the provider
__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$registry$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ProviderRegistry"].getInstance().registerProvider({
    name: "anthropic",
    serviceClass: AnthropicService,
    modelEnum: AnthropicModel
});
}}),
"[project]/src/lib/ai/providers/openai.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "OpenAIModel": (()=>OpenAIModel),
    "OpenAIService": (()=>OpenAIService)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$base$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/base.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$registry$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/registry.ts [app-route] (ecmascript)");
;
;
var OpenAIModel = /*#__PURE__*/ function(OpenAIModel) {
    OpenAIModel["GPT_4o"] = "gpt-4o";
    OpenAIModel["GPT_4o_MINI"] = "gpt-4o-mini";
    OpenAIModel["O1"] = "o1";
    OpenAIModel["O3_MINI"] = "o3-mini";
    return OpenAIModel;
}({});
class OpenAIService extends __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$base$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["BaseAIService"] {
    constructor(){
        const apiKeys = [
            process.env.HARVARD_OPENAI_API_KEY
        ].filter(Boolean);
        super(apiKeys, process.env.HARVARD_OPENAI_BASE_URL);
    }
    getModelName(model) {
        return model;
    }
    getDefaultModel() {
        return "gpt-4o";
    }
}
// Register the provider
__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$registry$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ProviderRegistry"].getInstance().registerProvider({
    name: "openai",
    serviceClass: OpenAIService,
    modelEnum: OpenAIModel
});
}}),
"[project]/src/lib/ai/providers/xai.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "XAIModel": (()=>XAIModel),
    "XAIService": (()=>XAIService)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$base$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/base.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$registry$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/registry.ts [app-route] (ecmascript)");
;
;
var XAIModel = /*#__PURE__*/ function(XAIModel) {
    XAIModel["GROK_2_LATEST"] = "grok-2-latest";
    return XAIModel;
}({});
class XAIService extends __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$base$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["BaseAIService"] {
    constructor(){
        const apiKeys = [
            process.env.XAI_API_KEY
        ].filter(Boolean);
        super(apiKeys, "https://api.x.ai/v1");
    }
    getModelName(model) {
        return model;
    }
    getDefaultModel() {
        return "grok-2-latest";
    }
}
// Register the provider
__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$registry$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ProviderRegistry"].getInstance().registerProvider({
    name: "xai",
    serviceClass: XAIService,
    modelEnum: XAIModel
});
}}),
"[project]/src/lib/ai/models.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "Models": (()=>Models)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$openai$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/openai.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$xai$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/xai.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$groq$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/groq.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$anthropic$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/anthropic.ts [app-route] (ecmascript)");
;
;
;
;
const Models = {
    OpenAI: __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$openai$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["OpenAIModel"],
    XAI: __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$xai$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["XAIModel"],
    Groq: __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$groq$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["GroqModel"],
    Anthropic: __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$anthropic$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["AnthropicModel"],
    // Add a method to get the provider name for a model
    getProviderName: (model)=>{
        // Check which provider's enum contains this model
        for (const [providerName, enumObj] of Object.entries(Models)){
            if (providerName !== 'getProviderName' && Object.values(enumObj).includes(model)) {
                return providerName;
            }
        }
        return "Unknown";
    }
};
}}),
"[project]/src/lib/ai/index.ts [app-route] (ecmascript) <locals>": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "AIService": (()=>AIService)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$registry$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/registry.ts [app-route] (ecmascript)");
;
;
;
;
;
;
;
;
;
;
class AIService {
    static instance;
    services = new Map();
    registry;
    constructor(){
        this.registry = __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$registry$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ProviderRegistry"].getInstance();
    }
    static getInstance() {
        if (!AIService.instance) {
            AIService.instance = new AIService();
        }
        return AIService.instance;
    }
    getService(model) {
        const provider = this.registry.getProviderByModel(model);
        if (!provider) {
            throw new Error(`No provider found for model: ${model}`);
        }
        let service = this.services.get(provider.name);
        if (!service) {
            service = new provider.serviceClass();
            this.services.set(provider.name, service);
        }
        return service;
    }
}
}}),
"[project]/src/lib/ai/index.ts [app-route] (ecmascript) <module evaluation>": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$registry$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/registry.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$groq$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/groq.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$anthropic$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/anthropic.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$openai$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/openai.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$providers$2f$xai$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/providers/xai.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/models.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/src/lib/ai/index.ts [app-route] (ecmascript) <locals>");
}}),
"[project]/src/lib/services/serverless/scanner/agents/vulnerability-agent.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "runVulnerabilityAgent": (()=>runVulnerabilityAgent)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$app$2f$api$2f$projects$2f5b$id$5d2f$repository$2f$content$2f$services$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/app/api/projects/[id]/repository/content/services.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$module__evaluation$3e$__ = __turbopack_context__.i("[project]/src/lib/ai/index.ts [app-route] (ecmascript) <module evaluation>");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/src/lib/ai/index.ts [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/models.ts [app-route] (ecmascript)");
;
;
/**
 * Builds a prompt for vulnerability detection based on specified parameters.
 *
 * @param path - Path of the file being analyzed
 * @param content - Source code content to analyze
 * @param vulnerabilityList - Optional list of specific vulnerabilities to check for
 * @param customVulnerabilities - Optional list of custom vulnerabilities to include
 * @returns A formatted prompt string for the AI model
 */ function buildVulnerabilityPrompt(path, content, vulnerabilityList = [], customVulnerabilities = []) {
    const codeLines = content.split("\n");
    const codeLinesWithLineNumbers = codeLines.map((line, index)=>`${index + 1}: ${line}`);
    const codeWithLineNumbers = codeLinesWithLineNumbers.join("\n");
    // Prepare custom vulnerabilities section if any are provided
    const customVulnerabilitiesSection = customVulnerabilities.length > 0 ? `

Additionally, check for these custom-defined vulnerabilities:
${customVulnerabilities.map((vuln, index)=>`${vulnerabilityList.length + index + 1}. ${vuln.name}: ${vuln.description}`).join('\n')}
` : '';
    return `

You are an expert security auditor tasked with identifying potential security vulnerabilities in the following code. 
Analyze this code thoroughly for security issues ${vulnerabilityList.length > 0 || customVulnerabilities.length > 0 ? "focusing specifically on these vulnerability types:" : "including but not limited to:"}

${vulnerabilityList.length > 0 ? vulnerabilityList.map((vuln, index)=>`${index + 1}. ${vuln}`).join("\n") : `1. Injection vulnerabilities (SQL, NoSQL, OS command, etc.)
      2. Authentication problems
      3. Session management flaws
      4. Insecure direct object references
      5. Security misconfiguration
      6. Cross-site scripting (XSS)
      7. Insecure deserialization
      8. Using components with known vulnerabilities
      9. Insufficient logging and monitoring
      10. Cross-site request forgery (CSRF)
      11. XML External Entities (XXE)
      12. Broken access control
      13. Sensitive data exposure
      14. Insecure crypto implementations
      15. Memory management issues (buffer overflows, memory leaks)
      16. Race conditions
      17. Hardcoded credentials or secrets`}${customVulnerabilitiesSection}

      File path: ${path}

      Code to analyze:
      \`\`\`
      ${codeWithLineNumbers}
      \`\`\`

      Identify any security vulnerabilities in the code. For each vulnerability, provide:
      - A descriptive title
      - The exact line number(s) affected (line_start and line_end)
      - A severity rating (Critical, High, Medium, Low)
      - A detailed explanation of the vulnerability
      - A code snippet of the vulnerable code
      - A recommended fix with code examples

IMPORTANT: respond _only_ with a valid JSON array of vulnerabilities. Do not include any explanatory text`;
}
/**
 * Parse the vulnerability response from any model and convert it to our database format
 */ function parseVulnerabilityResponse(completion, path, projectId, prompt) {
    let vulnerabilityResults = [];
    try {
        // Extract response content based on model type
        let responseContent = "";
        let parsedResponse;
        // All models should be compatible with this format (OpenAI core SDK)
        responseContent = completion.choices[0]?.message?.content || "";
        // Try tool calls first if available
        if (completion.choices[0]?.message?.tool_calls?.length) {
            const toolCall = completion.choices[0].message.tool_calls[0];
            parsedResponse = JSON.parse(toolCall.function.arguments);
        } else {
            // Fall back to content parsing
            parsedResponse = JSON.parse(responseContent.replace(/^```json\n?|```$/g, ""));
        }
        // Map the model's response to the database schema
        vulnerabilityResults = (parsedResponse.vulnerabilities || []).map((vuln)=>{
            // Convert severity to lowercase to match enum
            const severityMap = {
                "critical": "critical",
                "high": "high",
                "medium": "medium",
                "low": "low"
            };
            return {
                title: vuln.title,
                description: vuln.description,
                severity: severityMap[vuln.severity.toLowerCase()] || "medium",
                status: "open",
                location: path,
                file_path: path,
                line_start: vuln.line_start,
                line_end: vuln.line_end,
                code_snippet_lines: vuln.code_snippet_lines || [],
                remediation: vuln.remediation,
                project_id: projectId,
                detected_at: new Date().toISOString(),
                affected_components: [
                    path.split("/").pop() || ""
                ],
                reference_urls: null,
                cve: null,
                prompt: prompt
            };
        });
    } catch (error) {
        console.error("Failed to parse vulnerability results:", error);
    }
    return vulnerabilityResults;
}
/**
 * Apply the voting mechanism to determine which vulnerabilities have consensus
 */ function applyVotingMechanism(allResults, path, projectId, prompt) {
    // Map to hold vulnerabilities by a composite key for voting
    const vulnerabilityVotes = new Map();
    // Count how many models detected each vulnerability
    allResults.forEach(({ results, source })=>{
        results.forEach((vuln)=>{
            // Create a key to identify similar vulnerabilities
            // Using title + line numbers as a composite key
            const key = `${vuln.title}-${vuln.line_start}-${vuln.line_end}`;
            if (vulnerabilityVotes.has(key)) {
                // Increment vote for existing vulnerability
                const vote = vulnerabilityVotes.get(key);
                vote.votes++;
                vote.sources.push(source);
            } else {
                // Create new entry for this vulnerability
                vulnerabilityVotes.set(key, {
                    vulnerabilityKey: key,
                    votes: 1,
                    vulnerability: {
                        title: vuln.title,
                        line_start: vuln.line_start,
                        line_end: vuln.line_end,
                        severity: vuln.severity,
                        description: vuln.description,
                        remediation: vuln.remediation,
                        code_snippet_lines: vuln.code_snippet_lines
                    },
                    sources: [
                        source
                    ]
                });
            }
        });
    });
    // Extract votes into array
    const vulnArray = Array.from(vulnerabilityVotes.values());
    // Sort by number of votes (highest first)
    vulnArray.sort((a, b)=>b.votes - a.votes);
    // Filter to only include vulnerabilities with votes >= threshold (majority vote)
    // For 5 models, require at least 3 models to agree (majority)
    const consensusThreshold = Math.ceil(allResults.length / 2); // Majority threshold
    const consensusVulnerabilities = vulnArray.filter((v)=>v.votes >= consensusThreshold);
    console.log(`[VulnerabilityAgent] Voting results for ${path}:`);
    console.log(`- Total unique vulnerabilities found: ${vulnArray.length}`);
    console.log(`- Vulnerabilities with consensus (≥${consensusThreshold} votes): ${consensusVulnerabilities.length}`);
    // Convert back to Vulnerability[] format for database
    return consensusVulnerabilities.map((vote)=>{
        const vuln = vote.vulnerability;
        return {
            title: `${vuln.title} [${vote.votes}/${allResults.length} models]`,
            description: `${vuln.description}\n\n**Model Consensus:** ${vote.votes}/${allResults.length} models agreed (${vote.sources.join(', ')})`,
            severity: vuln.severity,
            status: "open",
            location: path,
            file_path: path,
            line_start: vuln.line_start,
            line_end: vuln.line_end,
            code_snippet_lines: vuln.code_snippet_lines || [],
            remediation: vuln.remediation,
            project_id: projectId,
            detected_at: new Date().toISOString(),
            affected_components: [
                path.split("/").pop() || ""
            ],
            reference_urls: null,
            cve: null,
            prompt: prompt
        };
    });
}
/**
 * Calls a single AI model to analyze vulnerabilities with error handling
 */ async function callModelWithErrorHandling(model, prompt) {
    try {
        const providerName = __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Models"].getProviderName(model);
        console.log(`[VulnerabilityAgent] Calling ${providerName} model ${model}`);
        return await callSingleModel(model, prompt);
    } catch (error) {
        console.error(`[VulnerabilityAgent] Error with ${__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Models"].getProviderName(model)} model ${model}:`, error);
        return null;
    }
}
/**
 * Calls a single AI model to analyze vulnerabilities
 */ async function callSingleModel(model, prompt) {
    // Use our new AI service
    const aiService = __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["AIService"].getInstance();
    const service = aiService.getService(model);
    // Call the model with the same parameters
    return await service.createCompletion(model, {
        messages: [
            {
                role: "user",
                content: prompt
            }
        ],
        temperature: 0.2,
        max_tokens: 4000,
        // response_format: { type: "json_object" },
        tools: [
            {
                type: "function",
                function: {
                    name: "report_vulnerabilities",
                    description: "Reports security vulnerabilities found in the code",
                    parameters: {
                        type: "object",
                        properties: {
                            vulnerabilities: {
                                type: "array",
                                items: {
                                    type: "object",
                                    properties: {
                                        title: {
                                            type: "string",
                                            description: "Title of the vulnerability"
                                        },
                                        line_start: {
                                            type: "integer",
                                            description: "Start line number of the vulnerability"
                                        },
                                        line_end: {
                                            type: "integer",
                                            description: "End line number of the vulnerability"
                                        },
                                        severity: {
                                            type: "string",
                                            description: "Severity level (Critical, High, Medium, Low)"
                                        },
                                        description: {
                                            type: "string",
                                            description: "Detailed description of the vulnerability"
                                        },
                                        code_snippet_lines: {
                                            type: "array",
                                            items: {
                                                type: "string",
                                                description: "Code snippet (without line number)"
                                            },
                                            description: "List of lines in code snippet showing the vulnerability"
                                        },
                                        remediation: {
                                            type: "string",
                                            description: "Recommended remediation steps"
                                        }
                                    },
                                    required: [
                                        "title",
                                        "line_start",
                                        "line_end",
                                        "severity",
                                        "description",
                                        "remediation",
                                        "code_snippet_lines"
                                    ]
                                }
                            }
                        },
                        required: [
                            "vulnerabilities"
                        ]
                    }
                }
            }
        ]
    });
}
async function runVulnerabilityAgent(projectId, branch, commit, path, vulnerabilityList = [], useVoting = true, customVulnerabilities = []) {
    const repoContent = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$app$2f$api$2f$projects$2f5b$id$5d2f$repository$2f$content$2f$services$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getFileContent"])({
        projectId,
        path,
        branch,
        commit
    });
    if ("error" in repoContent) {
        throw new Error(repoContent.error);
    }
    // If no file content, return empty array
    if (!repoContent.content) {
        return [];
    }
    // Build prompt using the separate function
    const prompt = buildVulnerabilityPrompt(path, repoContent.content, vulnerabilityList, customVulnerabilities);
    // If voting is disabled, use only the primary model
    if (!useVoting) {
        // We don't need to get a specific client anymore since our service handles that
        // Call the chosen model with the prompt
        const completion = await callSingleModel(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Models"].OpenAI.GPT_4o, prompt);
        return parseVulnerabilityResponse(completion, path, projectId, prompt);
    }
    // Use voting with multiple models in parallel
    console.log(`[VulnerabilityAgent] Using multi-model voting for ${path}`);
    // Define model configurations
    const modelConfigs = [
        {
            model: __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Models"].OpenAI.GPT_4o
        },
        // {model: Models.Anthropic.CLAUDE_3_5_SONNET},
        {
            model: __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Models"].XAI.GROK_2_LATEST
        }
    ];
    // Call all models in parallel
    const modelResults = await Promise.all(modelConfigs.map((config)=>callModelWithErrorHandling(config.model, prompt)));
    // Parse results from each model
    const vulnerabilityResults = modelResults.map((result, index)=>({
            results: result ? parseVulnerabilityResponse(result, path, projectId, prompt) : [],
            source: __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Models"].getProviderName(modelConfigs[index].model)
        }));
    // Apply voting mechanism
    return applyVotingMechanism(vulnerabilityResults, path, projectId, prompt);
}
}}),
"[project]/src/lib/services/serverless/scanner/agents/create-scan-summary.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "createScanSummary": (()=>createScanSummary)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$module__evaluation$3e$__ = __turbopack_context__.i("[project]/src/lib/ai/index.ts [app-route] (ecmascript) <module evaluation>");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/src/lib/ai/index.ts [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/ai/models.ts [app-route] (ecmascript)");
;
async function createScanSummary(scan) {
    console.log(`[ScanSummary] Creating summary for scan ${scan.id}`);
    // If no vulnerabilities, create a simple summary
    if (!scan.vulnerabilities || scan.vulnerabilities.length === 0) {
        console.log(`[ScanSummary] No vulnerabilities to summarize`);
        return {
            result_summary: "No vulnerabilities detected in this scan.",
            risk_score: 0
        };
    }
    console.log(`[ScanSummary] Summarizing ${scan.vulnerabilities.length} vulnerabilities`);
    // Get the AI service instance
    const aiService = __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$index$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["AIService"].getInstance();
    // Group vulnerabilities by severity
    const vulnsBySeverity = {
        critical: scan.vulnerabilities.filter((v)=>v.severity === "critical"),
        high: scan.vulnerabilities.filter((v)=>v.severity === "high"),
        medium: scan.vulnerabilities.filter((v)=>v.severity === "medium"),
        low: scan.vulnerabilities.filter((v)=>v.severity === "low")
    };
    console.log(`[ScanSummary] Severity breakdown - Critical: ${vulnsBySeverity.critical.length}, High: ${vulnsBySeverity.high.length}, Medium: ${vulnsBySeverity.medium.length}, Low: ${vulnsBySeverity.low.length}`);
    // Count vulnerabilities by type
    const vulnTypeCounts = {};
    scan.vulnerabilities.forEach((vuln)=>{
        const type = vuln.title.split(":")[0].trim();
        vulnTypeCounts[type] = (vulnTypeCounts[type] || 0) + 1;
    });
    console.log(`[ScanSummary] Vulnerability types: ${Object.keys(vulnTypeCounts).join(", ")}`);
    // Get top 5 most severe vulnerabilities for detailed report
    const topVulnerabilities = [
        ...vulnsBySeverity.critical,
        ...vulnsBySeverity.high,
        ...vulnsBySeverity.medium,
        ...vulnsBySeverity.low
    ].slice(0, 5);
    console.log(`[ScanSummary] Selected ${topVulnerabilities.length} top vulnerabilities for detailed report`);
    // Prepare detailed vulnerability information for the prompt
    const vulnDetails = topVulnerabilities.map((vuln, index)=>`
Vulnerability ${index + 1}:
- Title: ${vuln.title}
- Severity: ${vuln.severity}
- Location: ${vuln.file_path}:${vuln.line_start}-${vuln.line_end}
- Description: ${vuln.description}
- Code: ${vuln.code_snippet_lines ? vuln.code_snippet_lines.join('\n') : 'No code snippet available'}
- Remediation: ${vuln.remediation}
`).join("\n");
    console.log(`[ScanSummary] Building prompt for AI analysis`);
    // Construct the prompt for the AI
    const prompt = `
You are a security expert analyzing the results of a code security scan. Here's a summary of the scan:

Project ID: ${scan.project_id}
Files Scanned: ${scan.files_scanned}
Total Vulnerabilities: ${scan.vulnerabilities.length}
Severity Breakdown:
- Critical: ${vulnsBySeverity.critical.length}
- High: ${vulnsBySeverity.high.length}
- Medium: ${vulnsBySeverity.medium.length}
- Low: ${vulnsBySeverity.low.length}

Vulnerability Types:
${Object.entries(vulnTypeCounts).map(([type, count])=>`- ${type}: ${count}`).join("\n")}

Most Critical Vulnerabilities:
${vulnDetails}

Based on this information, please provide:
1. A concise executive summary (2-3 paragraphs) of the scan results
2. An assessment of the overall security risk
3. Prioritized recommendations for remediation

Format your response as a single continuous text with clear sections. Keep it factual, detailed but concise, and provide specific actionable insights.
`;
    try {
        console.log(`[ScanSummary] Calling AI service with OAI model`);
        // Get the OpenAI service from our AI service
        const openaiService = aiService.getService(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Models"].OpenAI.GPT_4o);
        // Call the AI model to generate the summary
        const completion = await openaiService.createCompletion(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$ai$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Models"].OpenAI.GPT_4o, {
            messages: [
                {
                    role: "user",
                    content: prompt
                }
            ],
            temperature: 0.3,
            max_tokens: 2000
        });
        const resultSummary = completion.choices[0]?.message?.content || "Failed to generate summary. Please review the raw scan results.";
        console.log(`[ScanSummary] Received AI response (${resultSummary.length} chars)`);
        console.log(`[ScanSummary] Summary preview: ${resultSummary.substring(0, 100)}...`);
        return {
            result_summary: resultSummary,
            // We calculate risk score in the main function, so just pass it through
            risk_score: scan.risk_score
        };
    } catch (error) {
        console.error("[ScanSummary] Error generating scan summary:", error);
        // Create a basic summary when AI fails
        const fallbackSummary = `Scan completed with ${scan.vulnerabilities.length} vulnerabilities found.
Critical: ${vulnsBySeverity.critical.length}, High: ${vulnsBySeverity.high.length}, 
Medium: ${vulnsBySeverity.medium.length}, Low: ${vulnsBySeverity.low.length}.
Could not generate detailed summary due to an error.`;
        console.log(`[ScanSummary] Using fallback summary due to API error`);
        return {
            result_summary: fallbackSummary,
            risk_score: scan.risk_score
        };
    }
}
}}),
"[project]/src/lib/services/serverless/scanner/scanner.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "calculateRiskScore": (()=>calculateRiskScore),
    "runVulnerabilityScan": (()=>runVulnerabilityScan)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/supabase/server.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$services$2f$serverless$2f$scanner$2f$agents$2f$dependency$2d$retriever$2d$agent$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/services/serverless/scanner/agents/dependency-retriever-agent.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$services$2f$serverless$2f$scanner$2f$agents$2f$vulnerability$2d$agent$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/services/serverless/scanner/agents/vulnerability-agent.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$services$2f$serverless$2f$scanner$2f$agents$2f$create$2d$scan$2d$summary$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/services/serverless/scanner/agents/create-scan-summary.ts [app-route] (ecmascript)");
;
;
;
;
/**
 * Sanitizes vulnerability data to prevent PostgreSQL errors with invalid Unicode characters.
 * Removes null characters and other problematic escape sequences.
 *
 * @param vulnerabilities Array of vulnerability objects to sanitize
 * @returns Sanitized array of vulnerability objects
 */ function sanitizeVulnerabilityData(vulnerabilities) {
    return vulnerabilities.map((vuln)=>{
        // Create a new object to avoid mutating the original
        const sanitized = {
            ...vuln
        };
        // Process all string properties to remove null characters and invalid escape sequences
        Object.keys(sanitized).forEach((key)=>{
            if (typeof sanitized[key] === "string") {
                // Replace null characters and other problematic escape sequences
                sanitized[key] = sanitized[key].replace(/\u0000/g, "") // Remove null characters
                .replace(/\\u0000/g, "") // Remove escaped null characters
                .replace(/[\uD800-\uDFFF]/g, "") // Remove unpaired surrogate code points
                .replace(/[\uFFFE\uFFFF]/g, ""); // Remove non-characters
            } else if (Array.isArray(sanitized[key]) && sanitized[key].every((item)=>typeof item === "string")) {
                // Handle arrays of strings (like code_snippet_lines)
                sanitized[key] = sanitized[key].map((str)=>str.replace(/\u0000/g, "").replace(/\\u0000/g, "").replace(/[\uD800-\uDFFF]/g, "").replace(/[\uFFFE\uFFFF]/g, ""));
            }
        // For nested objects we could add recursion, but it doesn't seem necessary for this schema
        });
        return sanitized;
    });
}
function calculateRiskScore(allVulnerabilities, filesScanned) {
    // Assign weights to each severity (tweakable without touching algorithm below)
    const severityWeights = {
        critical: 50,
        high: 30,
        medium: 20,
        low: 10
    };
    if (!allVulnerabilities.length) return 0;
    // 1. Count vulnerabilities by severity
    const severityCounts = {
        critical: 0,
        high: 0,
        medium: 0,
        low: 0
    };
    allVulnerabilities.forEach((v)=>{
        if (v.severity in severityCounts) severityCounts[v.severity]++;
    });
    // 2. Weighted base score
    let totalWeight = 0;
    let weightedSum = 0;
    Object.entries(severityCounts).forEach(([severity, count])=>{
        const weight = severityWeights[severity] || 0;
        weightedSum += count * weight;
        totalWeight += count;
    });
    const baseScore = weightedSum / (totalWeight || 1);
    // 3. Concentration – vulnerabilities packed in fewer files = higher risk
    const filesWithVulns = new Set(allVulnerabilities.map((v)=>v.file_path || "unknown")).size;
    const concentrationFactor = Math.min(1.0, Math.sqrt(totalWeight / (filesWithVulns || 1)));
    // 4. Coverage – percentage of scanned files with vulnerabilities
    const coverageFactor = filesWithVulns / (filesScanned || 1);
    // 5. Pattern – repeated vuln types suggest systemic issues
    const vulnTypeCount = {};
    allVulnerabilities.forEach((v)=>{
        const vulnType = v.title?.split(":")[0]?.trim() || "unknown";
        vulnTypeCount[vulnType] = (vulnTypeCount[vulnType] || 0) + 1;
    });
    const repeatedTypes = Object.values(vulnTypeCount).filter((c)=>c > 1).length;
    const patternFactor = repeatedTypes > 0 ? Math.min(1.3, 1 + repeatedTypes / 10) : 1.0;
    // 6. Severe impact – nonlinear weight for critical+high
    const severeCount = severityCounts.critical + severityCounts.high;
    const severeFactor = severeCount > 0 ? Math.min(1.5, 1 + Math.log10(severeCount + 1) / 2) : 1.0;
    // 7. Combine factors (weights sum to 1)
    const compoundFactor = severeFactor * 0.5 + concentrationFactor * 0.2 + coverageFactor * 0.1 + patternFactor * 0.2;
    // 8. Normalise to 0-100
    const adjusted = baseScore * compoundFactor;
    return Math.min(100, Math.ceil(adjusted * (1 + Math.log10(totalWeight + 1) / 2)));
}
async function runVulnerabilityScan(scanRequest) {
    const supabase = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createClient"])();
    const uniqueFilePaths = [
        ...new Set(scanRequest.filePaths)
    ];
    // Create a new scan record
    const timestamp = new Date().toISOString();
    const { data: newScan, error: insertError } = await supabase.from("scans").insert({
        project_id: scanRequest.projectId,
        branch: scanRequest.branch,
        commit_hash: scanRequest.commit,
        scan_type: scanRequest.scanType,
        status: "running",
        created_at: timestamp,
        started_at: timestamp,
        triggered_by: scanRequest.triggeredBy,
        progress_percentage: 0,
        files_scanned: 0,
        vulnerabilities_found: 0,
        current_stage: "initializing",
        execution_logs: "Scan initialized\n"
    }).select("id").single();
    if (insertError) {
        console.error("Error creating scan record:", insertError);
        throw new Error(`Failed to create scan record: ${insertError.message}`);
    }
    if (!newScan || !newScan.id) {
        throw new Error("Failed to create scan record");
    }
    const scanId = newScan.id;
    // Function to check if scan has been canceled
    const checkIfCanceled = async ()=>{
        const { data: currentScan } = await supabase.from("scans").select("status").eq("id", scanId).single();
        return currentScan?.status === "canceled";
    };
    // Update logs and stage
    const updateLogs = async (message, stage)=>{
        const { data: currentScan } = await supabase.from("scans").select("execution_logs, status").eq("id", scanId).single();
        // If scan doesn't exist or already marked as canceled, don't change the status
        if (!currentScan) {
            console.log(`[VulnerabilityScan] Scan ${scanId} no longer exists, cannot update logs`);
            return;
        }
        const isCanceled = currentScan.status === "canceled";
        // If already canceled and trying to change stage (not just adding logs), abort
        if (isCanceled && stage && stage !== "canceled") {
            console.log(`[VulnerabilityScan] Scan ${scanId} is canceled, not changing stage to ${stage}`);
            // Still append the log but don't change status or stage
            const logs = currentScan.execution_logs ? `${currentScan.execution_logs}${timestamp}: ${message} (Note: Scan is canceled)\n` : `${timestamp}: ${message} (Note: Scan is canceled)\n`;
            await supabase.from("scans").update({
                execution_logs: logs,
                updated_at: new Date().toISOString()
            }).eq("id", scanId);
            return;
        }
        const logs = currentScan.execution_logs ? `${currentScan.execution_logs}${timestamp}: ${message}\n` : `${timestamp}: ${message}\n`;
        const updateData = {
            execution_logs: logs,
            updated_at: new Date().toISOString()
        };
        // Only set stage if not canceled or if explicitly setting to canceled
        if (!isCanceled && stage) {
            updateData.current_stage = stage;
        } else if (stage === "canceled") {
            updateData.current_stage = "canceled";
            updateData.status = "canceled";
        }
        // If already canceled, preserve the canceled status
        if (isCanceled) {
            updateData.status = "canceled";
        }
        await supabase.from("scans").update(updateData).eq("id", scanId);
    };
    try {
        // Step 1: Expand file set by finding dependencies (skip for full scans)
        const isFullScan = scanRequest.scanType === "full";
        if (isFullScan) {
            await updateLogs("Full scan selected, skipping dependency analysis", "preparing_scan");
            console.log(`[VulnerabilityScan] Full scan ${scanId} for project ${scanRequest.projectId}, skipping dependency analysis`);
        } else {
            await updateLogs("Finding dependencies for target files...", "finding_dependencies");
            console.log(`[VulnerabilityScan] Starting scan ${scanId} for project ${scanRequest.projectId}`);
            console.log(`[VulnerabilityScan] Initial files to scan: ${uniqueFilePaths.length}`);
        }
        const expandedFilePaths = new Set(uniqueFilePaths);
        let progress = 0;
        // Only perform dependency analysis for non-full scans
        if (!isFullScan) {
            // For each file, find its dependencies and add them to the expanded set
            for (const filePath of uniqueFilePaths){
                // Check if scan has been canceled
                if (await checkIfCanceled()) {
                    console.log(`[VulnerabilityScan] Scan ${scanId} was canceled, stopping processing`);
                    await updateLogs("Scan canceled by user, stopping dependency analysis");
                    return {
                        ...newScan,
                        status: "canceled",
                        completed_at: new Date().toISOString(),
                        updated_at: new Date().toISOString()
                    };
                }
                try {
                    console.log(`[VulnerabilityScan] Finding dependencies for: ${filePath}`);
                    const dependencies = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$services$2f$serverless$2f$scanner$2f$agents$2f$dependency$2d$retriever$2d$agent$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getParentFiles"])(scanRequest.projectId, scanRequest.branch, scanRequest.commit, filePath);
                    console.log(`[VulnerabilityScan] Found ${dependencies.length} dependencies for ${filePath}`);
                    for (const dependency of dependencies){
                        expandedFilePaths.add(dependency);
                    }
                    progress++;
                    const progressPercentage = Math.floor(progress / uniqueFilePaths.length * 20); // 20% of total progress
                    await supabase.from("scans").update({
                        progress_percentage: progressPercentage,
                        updated_at: new Date().toISOString()
                    }).eq("id", scanId);
                } catch (error) {
                    console.error(`[VulnerabilityScan] Error finding dependencies for ${filePath}:`, error);
                    await updateLogs(`Failed to get dependencies for ${filePath}: ${error}`);
                // Continue with next file
                }
            }
            // Check again if scan was canceled after dependency analysis
            if (await checkIfCanceled()) {
                console.log(`[VulnerabilityScan] Scan ${scanId} was canceled, stopping processing`);
                await updateLogs("Scan canceled by user, stopping before file scanning");
                return {
                    ...newScan,
                    status: "canceled",
                    completed_at: new Date().toISOString(),
                    updated_at: new Date().toISOString()
                };
            }
        }
        const allFilesToScan = Array.from(expandedFilePaths);
        if (!isFullScan) {
            console.log(`[VulnerabilityScan] Total files to scan after dependency analysis: ${allFilesToScan.length}`);
            await updateLogs(`Found ${allFilesToScan.length} files to scan (${allFilesToScan.length - uniqueFilePaths.length} dependencies added)`);
        } else {
            console.log(`[VulnerabilityScan] Total files to scan: ${allFilesToScan.length}`);
            await updateLogs(`Ready to scan ${allFilesToScan.length} files`);
        }
        // Step 2: Scan each file for vulnerabilities
        await updateLogs("Starting vulnerability scan...", "scanning_files");
        console.log(`[VulnerabilityScan] Beginning vulnerability scanning phase for ${allFilesToScan.length} files`);
        const allVulnerabilities = [];
        let filesScanned = 0;
        // Create a function to process a single file
        const scanFile = async (filePath)=>{
            try {
                console.log(`[VulnerabilityScan] Scanning file: ${filePath}`);
                const vulnerabilities = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$services$2f$serverless$2f$scanner$2f$agents$2f$vulnerability$2d$agent$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["runVulnerabilityAgent"])(scanRequest.projectId, scanRequest.branch, scanRequest.commit, filePath, scanRequest.vulnerabilityList, false, scanRequest.customVulnerabilities || []);
                if (vulnerabilities.length > 0) {
                    console.log(`[VulnerabilityScan] Found ${vulnerabilities.length} vulnerabilities in ${filePath}`);
                    // Add scan_id to each vulnerability
                    return vulnerabilities.map((v)=>({
                            ...v,
                            scan_id: scanId,
                            created_at: new Date().toISOString(),
                            updated_at: new Date().toISOString()
                        }));
                } else {
                    console.log(`[VulnerabilityScan] No vulnerabilities found in ${filePath}`);
                    return [];
                }
            } catch (error) {
                console.error(`[VulnerabilityScan] Error scanning ${filePath}:`, error);
                await updateLogs(`Error scanning ${filePath}: ${error}`, "error");
                return [];
            }
        };
        // Task queue implementation for processing files
        // Maintains at most MAX_CONCURRENT_TASKS running at a time
        const MAX_CONCURRENT_TASKS = 8;
        let activeTasks = 0;
        let taskIndex = 0;
        // Process files through a task queue
        const processTaskQueue = async ()=>{
            const allResults = [];
            // Process files with a concurrent task queue
            const processNextTask = async ()=>{
                // Check for cancellation periodically
                if (taskIndex % 5 === 0 && await checkIfCanceled()) {
                    console.log(`[VulnerabilityScan] Scan ${scanId} was canceled, stopping task queue`);
                    await updateLogs("Scan canceled by user, stopping vulnerability scan");
                    throw new Error("SCAN_CANCELED");
                }
                // If all tasks are queued, we're done
                if (taskIndex >= allFilesToScan.length) return;
                // Get the next file to process
                const filePath = allFilesToScan[taskIndex++];
                activeTasks++;
                try {
                    // Process the file
                    const vulnerabilities = await scanFile(filePath);
                    // Insert vulnerabilities if found
                    if (vulnerabilities.length > 0) {
                        // Sanitize the vulnerability data before insertion
                        const sanitizedVulnerabilities = sanitizeVulnerabilityData(vulnerabilities);
                        const { error: vulnInsertError } = await supabase.from("vulnerabilities").insert(sanitizedVulnerabilities);
                        if (vulnInsertError) {
                            console.error(`[VulnerabilityScan] Error inserting vulnerabilities:`, vulnInsertError);
                            await updateLogs(`Error inserting vulnerabilities: ${vulnInsertError.message}`);
                        } else {
                            await updateLogs(`Found ${vulnerabilities.length} vulnerabilities in ${filePath}`);
                            allResults.push(...vulnerabilities);
                        }
                    }
                    // Update progress
                    filesScanned++;
                    const progressPercentage = 20 + Math.floor(filesScanned / allFilesToScan.length * 70); // 20-90% of total progress
                    await supabase.from("scans").update({
                        progress_percentage: progressPercentage,
                        files_scanned: filesScanned,
                        vulnerabilities_found: allResults.length,
                        updated_at: new Date().toISOString()
                    }).eq("id", scanId);
                } catch (error) {
                    console.error(`[VulnerabilityScan] Error in task for ${filePath}:`, error);
                } finally{
                    activeTasks--;
                    // Start next task
                    if (taskIndex < allFilesToScan.length) {
                        await processNextTask();
                    }
                }
            };
            // Start the initial batch of tasks
            const initialTasks = [];
            const initialBatchSize = Math.min(MAX_CONCURRENT_TASKS, allFilesToScan.length);
            for(let i = 0; i < initialBatchSize; i++){
                initialTasks.push(processNextTask());
            }
            // Wait for all tasks to complete
            await Promise.all(initialTasks);
            // Wait until all active tasks are done
            while(activeTasks > 0){
                await new Promise((resolve)=>setTimeout(resolve, 100));
            }
            return allResults;
        };
        // Execute the task queue
        try {
            await updateLogs(`Starting vulnerability scanning with up to ${MAX_CONCURRENT_TASKS} concurrent files`);
            const scanResults = await processTaskQueue();
            allVulnerabilities.push(...scanResults);
        } catch (error) {
            if (error instanceof Error && error.message === "SCAN_CANCELED") {
                return {
                    ...newScan,
                    status: "canceled",
                    completed_at: new Date().toISOString(),
                    updated_at: new Date().toISOString(),
                    files_scanned: filesScanned,
                    vulnerabilities_found: allVulnerabilities.length
                };
            }
            console.error(`[VulnerabilityScan] Error processing task queue:`, error);
            await updateLogs(`Error in vulnerability scanning: ${error instanceof Error ? error.message : String(error)}`);
        }
        // Check again if scan was canceled before generating summary
        if (await checkIfCanceled()) {
            console.log(`[VulnerabilityScan] Scan ${scanId} was canceled, stopping before report generation`);
            await updateLogs("Scan canceled by user, stopping before report generation");
            return {
                ...newScan,
                status: "canceled",
                completed_at: new Date().toISOString(),
                updated_at: new Date().toISOString(),
                files_scanned: filesScanned,
                vulnerabilities_found: allVulnerabilities.length
            };
        }
        // Step 3: Generate summary
        await updateLogs("Generating scan report...", "generating_report");
        console.log(`[VulnerabilityScan] Beginning summary generation for scan ${scanId}`);
        console.log(`[VulnerabilityScan] Total vulnerabilities found: ${allVulnerabilities.length}`);
        // Collect vulnerability types
        const vulnerabilityTypes = new Set();
        allVulnerabilities.forEach((v)=>{
            vulnerabilityTypes.add(v.title.split(":")[0].trim());
        });
        console.log(`[VulnerabilityScan] Vulnerability types found: ${Array.from(vulnerabilityTypes).join(", ")}`);
        let riskScore = calculateRiskScore(allVulnerabilities, filesScanned);
        // Create detailed summary
        const scanRow = {
            ...newScan,
            progress_percentage: 90,
            status: "completed",
            files_scanned: filesScanned,
            vulnerabilities_found: allVulnerabilities.length,
            vulnerability_types: Array.from(vulnerabilityTypes),
            risk_score: riskScore,
            completed_at: new Date().toISOString(),
            updated_at: new Date().toISOString()
        };
        // Generate comprehensive report using AI
        console.log(`[VulnerabilityScan] Calling AI for comprehensive report generation`);
        const scanSummary = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$services$2f$serverless$2f$scanner$2f$agents$2f$create$2d$scan$2d$summary$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createScanSummary"])({
            ...scanRow,
            vulnerabilities: allVulnerabilities
        });
        console.log(`[VulnerabilityScan] AI summary generated (${scanSummary.result_summary?.length || 0} chars)`);
        // Update the scan record with the final summary
        const finalScanData = {
            ...scanSummary,
            progress_percentage: 100,
            status: "completed",
            current_stage: "completed",
            updated_at: new Date().toISOString()
        };
        const { error: updateError } = await supabase.from("scans").update(finalScanData).eq("id", scanId);
        if (updateError) {
            console.error(`[VulnerabilityScan] Error updating final scan data:`, updateError);
            await updateLogs(`Error updating final scan data: ${updateError.message}`);
        } else {
            console.log(`[VulnerabilityScan] Scan ${scanId} completed successfully`);
            await updateLogs("Scan completed successfully", "completed");
        }
        // Return the complete scan row
        return {
            ...scanRow,
            ...finalScanData
        };
    } catch (error) {
        // Handle errors in the scan process
        const errorMessage = error instanceof Error ? error.message : "Unknown error";
        console.error(`[VulnerabilityScan] Scan ${scanId} failed:`, error);
        await updateLogs(`Scan failed: ${errorMessage}`, "failed");
        await supabase.from("scans").update({
            status: "failed",
            current_stage: "failed",
            result_summary: `Scan failed: ${errorMessage}`,
            updated_at: new Date().toISOString(),
            completed_at: new Date().toISOString()
        }).eq("id", scanId);
        throw error;
    }
}
}}),
"[project]/src/app/api/projects/[id]/scans/route.ts [app-route] (ecmascript)": ((__turbopack_context__) => {
"use strict";

var { g: global, __dirname } = __turbopack_context__;
{
__turbopack_context__.s({
    "GET": (()=>GET),
    "POST": (()=>POST),
    "mockScanQueue": (()=>mockScanQueue)
});
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$services$2f$serverless$2f$scanner$2f$scanner$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/services/serverless/scanner/scanner.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/supabase/server.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/server.js [app-route] (ecmascript)");
;
;
;
const mockScanQueue = new Map();
async function GET(req, { params }) {
    try {
        const awaitedParams = await params;
        // Authenticate the user
        const supabase = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createClient"])();
        const { data: { user } } = await supabase.auth.getUser();
        if (!user) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: "Authentication required"
            }, {
                status: 401
            });
        }
        const projectId = awaitedParams.id;
        // Fetch all scans for this project
        const { data, error } = await supabase.from("scans").select("*").eq("project_id", projectId).order("created_at", {
            ascending: false
        });
        if (error) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: `Failed to fetch scans: ${error.message}`
            }, {
                status: 500
            });
        }
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json(data);
    } catch (error) {
        console.error("Error fetching scans:", error);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "Failed to fetch scans"
        }, {
            status: 500
        });
    }
}
async function POST(req, { params }) {
    try {
        const awaitedParams = await params;
        // Get authentication from Supabase
        const supabase = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createClient"])();
        const { data: { session } } = await supabase.auth.getSession();
        if (!session?.user) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: "Authentication required"
            }, {
                status: 401
            });
        }
        const userId = session.user.id;
        const projectId = awaitedParams.id;
        const body = await req.json();
        // Validate required fields
        if (!body.scan_type) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: "Missing required field: scan_type"
            }, {
                status: 400
            });
        }
        // Map legacy scan types to valid database enum values
        const mapScanType = (type)=>{
            switch(type){
                case "deep":
                    return "full";
                case "commit":
                    return "incremental";
                case "custom":
                    return "targeted";
                default:
                    return type;
            }
        };
        // Prepare vulnerabilityList from vulnerability_types
        const vulnerabilityList = body.vulnerability_types || [];
        // Extract custom vulnerabilities if provided
        const customVulnerabilities = body.custom_vulnerabilities || [];
        // Prepare scan request with mapped scan type
        const scanRequest = {
            projectId,
            branch: body.branch || "main",
            commit: body.commit_hash || null,
            filePaths: body.files || [],
            triggeredBy: userId,
            scanType: mapScanType(body.scan_type),
            vulnerabilityList,
            customVulnerabilities
        };
        try {
            // Start the vulnerability scan process
            console.log(`Starting scan for project ${projectId}`);
            // Fire and forget - run the scan without awaiting or storing the promise
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$services$2f$serverless$2f$scanner$2f$scanner$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["runVulnerabilityScan"])(scanRequest);
            // Return an initial response to the client
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                status: "pending",
                message: "Scan started successfully"
            });
        } catch (error) {
            console.error("Error processing scan request:", error);
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: "Failed to process scan request"
            }, {
                status: 500
            });
        }
    } catch (error) {
        console.error("Error processing scan request:", error);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "Failed to process scan request"
        }, {
            status: 500
        });
    }
}
}}),

};

//# sourceMappingURL=%5Broot%20of%20the%20server%5D__933142dc._.js.map